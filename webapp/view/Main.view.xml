<mvc:View controllerName="userprofile.controller.Main"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
	xmlns:l="sap.ui.layout"
	xmlns:f="sap.ui.layout.form"
    xmlns="sap.m">
    <Page id="page" title="{i18n>title}">
        <content>
			<VBox class="sapUiSmallMargin">
				<f:SimpleForm id="SimpleFormDisplay354"
					editable="false"
					layout="ResponsiveGridLayout"
					labelSpanXL="3"
					labelSpanL="3"
					labelSpanM="3"
					labelSpanS="12"
					adjustLabelSpan="false"
					emptySpanXL="4"
					emptySpanL="4"
					emptySpanM="4"
					emptySpanS="0"
					columnsXL="1"
					columnsL="1"
					columnsM="1"
					singleContainerFullSize="false" >
					<f:content>
						<Label text="Name" />
						<Text text="{/FirstName} {/LastName}" />
						<Label text="Title" />
						<Text text="{/Title}" />
						<Label text="Address" />
						<Text text="{/Address}" />
						<Label text="City" />
						<Text text="{/City}" />
						<Label text="Country" />
						<Text text="{/Country}" />
						<Label text="Postal Code" />
						<Text text="{/PostalCode}" />
					</f:content>
				</f:SimpleForm>
			</VBox>
        </content>
    </Page>
</mvc:View>
